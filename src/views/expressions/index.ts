// Export only the dispatcher functions
export { dispatchExpression, dispatchPattern } from './dispatchers';

// Export types if needed
export type { ExpressionProps, ExpressionComponent } from './components';
